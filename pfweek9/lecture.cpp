#include <iostream>
using namespace std;
main()
{
    /*int arrSize = 4;
    float marks[arrSize]={78 , 21.9 , 38 , 46};
    for(int index = 0 ; index < arrSize; index++)
    {
        cout<<"marks of student " << index+1<<" are : "<< marks[index] << endl;
    }
    float marks[arrSize];*/

    /*for(int index = 0 ; index < arrSize; index++)
    {
        cout<<"Enter marks of student " << index+1<<" : ";
        cin >> marks[index];
    }
    for(int index = 0 ; index < arrSize; index++)
    {
        cout<<"marks of student " << index+1<<" are : "<< marks[index] << endl;
    }*/

    /*string word;
    
    cout<<"enter word : ";
    getline(cin, word);
    
    cout<< word;*/
}